namespace LWFlo {
    public enum ScopeName {
        AppScope,
        MenuScope,
        GameplayScope
    }
    
    public enum AppState
    {
        Initializing,
        Loading,
        Playing,
        Paused,
        Error,
        Quitting
    }
}
